# frozen_string_literal: true

guard 'livereload' do
  watch(%r{app/(components|views)/.+\.(erb|haml|slim)$})
  watch(%r{app/helpers/.+\.rb})
  watch(%r{public/.+\.(css|js|html)})
  watch(%r{config/locales/.+\.yml})
end
